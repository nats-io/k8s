apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  labels:
    {{- include "nats.labels" $ | nindent 4 }}
data:
  nats.conf: |
    {{- include "nats.formatConfig" .config | nindent 4 }}
