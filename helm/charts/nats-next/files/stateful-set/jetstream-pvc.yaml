{{- $ := . }}
{{- with .Values.config.jetstream.fileStore.pvc }}
metadata:
  name: {{ .name | default (printf "%s-js" (include "nats.fullname" $)) }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .size | quote }}
  {{- with .storageClassName }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
