{{- $ := . }}
{{- with .Values.config.jetstream.fileStore.pvc }}
metadata:
  name: {{ include "nats.fullname" $ }}-js
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .size | quote }}
  {{- with .storageClassName }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
