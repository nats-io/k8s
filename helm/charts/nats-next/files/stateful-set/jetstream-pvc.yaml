{{- $ := . }}
{{- with .Values.config.jetstream.fileStorage.pvc }}
metadata:
  name: {{ include "nats.fullname" $ }}-js-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ $.Values.config.jetstream.fileStorage.size | quote }}
  {{- with .storageClassName }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
