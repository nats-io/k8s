{{- $ := . }}
{{- with .Values.resolverPvc }}
metadata:
  name: {{ include "nats.fullname" $ }}-resolver-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .size | quote }}
  {{- with .storageClassName }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
